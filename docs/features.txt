* = required

current obtainable features:
    *chat message:
        *timestamp  -> json['data'][str(index)]['attributes']['timestamp']
        *content    -> json['data'][str(index)]['attributes']['message']
        deleted     -> json['data'][str(index)]['attributes']['deleted']
        user        -> json['data'][str(index)]['attributes']['from']

unobtained desired features:
    number of viewers

feature data format:
    --

This will be a classifier that classifies whether something notable happened on stream or not.
Therefore, it will be classifying a GROUP of messages, rather than a single message.
The high level features should be:
    number of unique users that posted
    the rate at which new messages are appearing relative to the number of viewers
    ...
